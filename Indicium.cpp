/**
* Problem Link: 
*/
#include <bits/stdc++.h>

using namespace std;

vector<vector<int> > generate_latin_square(int n) {
    vector<vector<int> > square(n, vector<int> (n));
}

int main() {
    freopen("in", "r", stdin), freopen("out", "w", stdout);
    ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    vector<vector<int> > squares = generate_latin_square(5);
    return 0;
}