#include <bits/stdc++.h>

using namespace std;

const int inf = 1e9;

int main() {
	freopen("in", "r", stdin);
	ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
	int r1, c1, r2, c2;
	cin >> r1 >> c1 >> r2 >> c2;
	if()
	if(ans < 0) cout << -1 << "\n";
	else cout << ans;
	return 0;
}