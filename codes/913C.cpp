#include <bits/stdc++.h>

using namespace std;

int c[30];

int main() {
	freopen("in", "r", stdin), freopen("out", "w", stdout);
	int n, l;
	scanf("%d %d", &n, &l);
	for(int i = 0; i < n; ++i) scanf("%d", c + i);
	return 0;
}