#include <bits/stdc++.h>

using namespace std;

int C[1001];

int main() {
	freopen("in","r",stdin);
	ios::sync_with_stdio(0);
	cin.tie(0);
	int n,val;
	cin >> n;
	for(int i=0; i<n; ++i) {
		cin >> val;
		++C[val];
	}
	for(int i=1; i<=1000; ++i) {
		for(int )
	}
	cout << ans << "\n";
	return 0;
}