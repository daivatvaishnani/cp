#include <bits/stdc++.h>

using namespace std;

const int maxn = 1e5 + 5;

int a[maxn];

int main() {
	int t, n, val;
	scanf("%d", &t);
	while(t--) {
		scanf("%d", &n);
		for(int i = 1; i < n + 1; ++i) {
			scanf("%d", &a[i]);
		}
		
	}
	return 0;
}