#include <bits/stdc++.h>

using namespace std;

map< pair<int, int>, int> M;

int main() {
	int T,N,Q,i,j,val;
	cin >> T;
	while(T--) {
		cin >> N >> Q;
		while(Q--) {
			cin >> i >> j >> val;
			if(M.find(make_pair(i,j))==M.end()) {
				
			}
			else {

			}
		}
	}
	return 0;
}