#include <bits/stdc++.h>

using namespace std;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int a,b,c;
	string s;

	cin >> a >> b >> c >> s;

	cout << a + b + c << " " << s << "\n";

	return 0;
}